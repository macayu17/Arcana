import { StudyDashboard } from "@/components/study/study-dashboard";

export const metadata = {
  title: "Study",
  description: "Arcana study progress dashboard and confidence map.",
};

export default function StudyPage() {
  return <StudyDashboard />;
}
